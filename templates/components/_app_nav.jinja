{% load static %}
<head>
    <script>
        // Tema kontrolü için JavaScript
        document.addEventListener('DOMContentLoaded', function() {
            const themeToggleBtn = document.getElementById('theme-toggle');
            const htmlElement = document.documentElement;
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');
            const userMenuButton = document.getElementById('user-menu-button');
            const userDropdown = document.getElementById('user-dropdown');
            
            // Yerel depolamadan tema tercihini kontrol et
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme) {
                htmlElement.setAttribute('data-theme', savedTheme);
                updateButtonIcon(savedTheme);
            }
            
            // Tema değiştirme butonuna tıklama olayı
            themeToggleBtn.addEventListener('click', function() {
                let currentTheme = htmlElement.getAttribute('data-theme') || 'light';
                let newTheme = currentTheme === 'light' ? 'dark' : 'light';
                
                htmlElement.setAttribute('data-theme', newTheme);
                localStorage.setItem('theme', newTheme);
                updateButtonIcon(newTheme);
            });
            
            // Mobil menü butonuna tıklama olayı
            mobileMenuButton.addEventListener('click', function() {
                mobileMenu.classList.toggle('hidden');
            });
            
            // Kullanıcı menüsü butonuna tıklama olayı
            userMenuButton.addEventListener('click', function(e) {
                e.stopPropagation();
                userDropdown.classList.toggle('hidden');
                userDropdown.classList.toggle('animate-dropdown');
            });
            
            // Dışarı tıklandığında menüleri kapat
            document.addEventListener('click', function(event) {
                if (!userMenuButton.contains(event.target) && !userDropdown.contains(event.target)) {
                    userDropdown.classList.add('hidden');
                    userDropdown.classList.remove('animate-dropdown');
                }
                if (!mobileMenuButton.contains(event.target) && !mobileMenu.contains(event.target) && window.innerWidth < 768) {
                    mobileMenu.classList.add('hidden');
                }
            });
            
            // Buton ikonunu güncelleme fonksiyonu
            function updateButtonIcon(theme) {
                const icon = themeToggleBtn.querySelector('i');
                if (theme === 'dark') {
                    icon.className = 'fas fa-sun';
                } else {
                    icon.className = 'fas fa-moon';
                }
            }
        });
    </script>
    <style>
        /* Temel tema stilleri */
        :root {
            --bg-color: #ffffff;
            --text-color: #111827;
            --border-color: #e5e7eb;
            --hover-bg: #f9fafb;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }
        
        [data-theme="dark"] {
            --bg-color: #1f2937;
            --text-color: #f3f4f6;
            --border-color: #374151;
            --hover-bg: #374151;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.4), 0 2px 4px -1px rgba(0, 0, 0, 0.3);
        }
        
        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: background-color 0.3s, color 0.3s;
        }
        
        .nav-bg {
            background-color: var(--bg-color);
            border-bottom: 1px solid var(--border-color);
        }
        
        .text-primary {
            color: var(--text-color);
        }
        
        /* Tema butonu stilleri */
        .theme-toggle {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 2.5rem;
            height: 2.5rem;
            border-radius: 50%;
            background-color: var(--hover-bg);
            border: 1px solid var(--border-color);
            cursor: pointer;
            transition: all 0.3s;
            margin-right: 0.75rem;
        }
        
        .theme-toggle:hover {
            background-color: var(--border-color);
            transform: rotate(12deg);
        }
        
        /* Dropdown stilleri - Animasyonlu */
        .dropdown-menu {
            position: absolute;
            right: 1rem;
            top: 3.5rem;
            width: 18rem;
            background-color: var(--bg-color);
            border-radius: 0.5rem;
            box-shadow: var(--shadow);
            border: 1px solid var(--border-color);
            z-index: 50;
            overflow: hidden;
            opacity: 0;
            transform: scale(0.95) translateY(-10px);
            transform-origin: top right;
            transition: opacity 0.2s ease, transform 0.2s ease, visibility 0.2s;
            visibility: hidden;
        }
        
        .dropdown-menu.animate-dropdown {
            opacity: 1;
            transform: scale(1) translateY(0);
            visibility: visible;
        }
        
        .dropdown-item {
            display: flex;
            align-items: center;
            padding: 0.6rem 1rem;
            color: var(--text-color);
            transition: all 0.2s ease;
            position: relative;
        }
        
        .dropdown-item:hover {
            background-color: var(--hover-bg);
            padding-left: 1.2rem;
        }
        
        .dropdown-item:hover i {
            transform: translateX(3px);
        }
        
        .dropdown-item i {
            transition: transform 0.2s ease;
        }
        
        /* Sol kenar çizgisi efekti */
        .dropdown-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            width: 3px;
            background: linear-gradient(to bottom, #3b82f6, #8b5cf6);
            opacity: 0;
            transition: opacity 0.2s ease;
        }
        
        .dropdown-item:hover::before {
            opacity: 1;
        }
        
        /* Mobil menü stilleri */
        .mobile-menu {
            display: none;
            background-color: var(--bg-color);
            border-top: 1px solid var(--border-color);
            padding: 1rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }
        
        @media (max-width: 767px) {
            .mobile-menu {
                display: block;
            }
            
            .desktop-only {
                display: none;
            }
        }
        
        @media (min-width: 768px) {
            .mobile-only {
                display: none;
            }
        }
        
        /* Kullanıcı avatarı animasyonu */
        .user-avatar {
            transition: all 0.3s ease;
        }
        
        .user-avatar:hover {
            transform: scale(1.05);
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.3);
        }
    </style>
    <!-- Font Awesome CDN for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <header class="antialiased sticky top-0 z-50">
        <nav class="nav-bg px-4 lg:px-6 py-3">
            <div class="flex justify-between items-center">
                <!-- Logo ve mobil menü butonu -->
                <div class="flex items-center">
                    <!-- Mobil menü butonu -->
                    <button id="mobile-menu-button" class="mobile-only mr-3 text-primary focus:outline-none">
                    </button>
                    
                    <!-- Logo -->
                    <a href="/uygulama/" class="flex items-center">
                        <img src="{% static 'images/logo.png' %}" class="h-12 w-auto" alt="Logo" />
                    </a>
                </div>

                <!-- Sağ taraf - tema değiştirici ve kullanıcı menüsü -->
                <div class="flex items-center">
                    <!-- Tema değiştirme butonu -->
                    <button id="theme-toggle" class="theme-toggle" style="display: none;">
                        <i class="fas fa-moon"></i>
                    </button>
                    
                    <!-- Kullanıcı menüsü butonu -->
                    <button type="button" class="flex text-sm rounded-full md:mr-0 focus:outline-none focus:ring-2 focus:ring-blue-500" id="user-menu-button">
                        <span class="sr-only">Kullanıcı menüsünü aç</span>
                        <img class="w-9 h-9 rounded-full border-2 border-transparent user-avatar" src="{% if request.user.avatar %}{{request.user.avatar.url}}{% else %}{% static 'images/blank-profile.jpg' %}{% endif %}" alt="user">
                    </button>
                </div>
            </div>

      
        </nav>

        <!-- Kullanıcı dropdown menüsü (masaüstü) - Animasyonlu -->
        <div class="dropdown-menu hidden" id="user-dropdown">
            <div class="py-3 px-4 border-b border-gray-200 dark:border-gray-600">
                <span class="block text-sm font-semibold text-primary">{{request.user.first_name}} {{request.user.last_name}}</span>
                <span class="block text-sm text-gray-500 truncate">{{request.user.email}}</span>
            </div>
            <ul class="py-1">
                <li>
                    <a href="/uygulama/profil/" class="dropdown-item">
                        <i class="fas fa-user mr-3 w-5 text-center text-blue-500"></i> Profilim
                    </a>
                </li>
                <li>
                    <a href="/uygulama/alici-talepleri/" class="dropdown-item">
                        <i class="fas fa-list-alt mr-3 w-5 text-center text-green-500"></i> Alıcı Talepleri
                    </a>
                </li>
                <li>
                    <a href="/mesajlar/" class="dropdown-item">
                        <i class="fas fa-envelope mr-3 w-5 text-center text-yellow-500"></i> Mesajlarım
                    </a>
                </li>
                <li>
                    <a href="/uygulama/profil/duzenle/" class="dropdown-item">
                        <i class="fas fa-cog mr-3 w-5 text-center text-gray-500"></i> Ayarlar
                    </a>
                </li>
            </ul>
            <ul class="py-1 border-t border-gray-200 dark:border-gray-600">
                <li>
                    <a href="/ticket/" class="dropdown-item">
                        <i class="fas fa-question-circle mr-3 w-5 text-center text-purple-500"></i> Destek
                    </a>
                </li>
            </ul>
            <ul class="py-1 border-t border-gray-200 dark:border-gray-600">
                <li>
                    <a href="/uygulama/cikis-yap/" class="dropdown-item text-red-600">
                        <i class="fas fa-sign-out-alt mr-3 w-5 text-center"></i> Çıkış Yap
                    </a>
                </li>
            </ul>
        </div>
    </header>
</body>
</html>